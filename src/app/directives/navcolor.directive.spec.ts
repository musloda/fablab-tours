import { NavcolorDirective } from './navcolor.directive';

describe('NavcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new NavcolorDirective();
    expect(directive).toBeTruthy();
  });
});
